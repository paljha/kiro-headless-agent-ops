apiVersion: v1
kind: Service
metadata:
  name: fastapi-demo
  namespace: fastapi-demo
spec:
  type: ClusterIP
  selector:
    app: fastapi-demo
  ports:
    - name: http
      port: 80
      targetPort: 8000
